11.

cpp
Copiar
Editar
#include <iostream>
using namespace std;

int main() {
    int a = 10;
    int* p = &a;
    cout << "Valor: " << *p << ", Endereço: " << p << endl;
    return 0;
}
12.

cpp
Copiar
Editar
void dobrar(int* p) {
    *p *= 2;
}
13.

cpp
Copiar
Editar
void trocar(int* a, int* b) {
    int temp = *a;
    *a = *b;
    *b = temp;
}
14.

cpp
Copiar
Editar
void exibirVetor(int* v, int tamanho) {
    for(int i = 0; i < tamanho; i++)
        cout << v[i] << " ";
    cout << endl;
}
15.

cpp
Copiar
Editar
void verificarNota(float* nota) {
    if (*nota >= 7)
        cout << "Aprovado" << endl;
    else
        cout << "Reprovado" << endl;
}
