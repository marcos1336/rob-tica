26.

cpp
Copiar
Editar
void menu() {
    int op, a, b;
    cout << "1-Soma 2-Sub 3-Mult: ";
    cin >> op >> a >> b;
    if (op == 1) cout << a + b;
    else if (op == 2) cout << a - b;
    else if (op == 3) cout << a * b;
}
27.

cpp
Copiar
Editar
#define LIMITE 1000

float saldo = 0;

void saque(float valor) {
    if(valor <= saldo) saldo -= valor;
}

void deposito(float valor) {
    if(valor <= LIMITE) saldo += valor;
}

void consulta() {
    cout << "Saldo: " << saldo << endl;
}
28.

cpp
Copiar
Editar
struct Pessoa {
    string nome;
    int idade;
    float altura;
};

void imprimir(Pessoa* p) {
    cout << p->nome << " " << p->idade << " " << p->altura << endl;
}
29.

cpp
Copiar
Editar
template <typename T>
void trocarPonteiros(T* a, T* b) {
    T temp = *a;
    *a = *b;
    *b = temp;
}
30. (Exemplo simples integrando múltiplos conceitos)

Arquivo funcoes.h

cpp
Copiar
Editar
#pragma once
void trocar(int&, int&);
void incrementar(int*);
Arquivo funcoes.cpp

cpp
Copiar
Editar
#include "funcoes.h"

void trocar(int &a, int &b) {
    int temp = a;
    a = b;
    b = temp;
}

void incrementar(int* p) {
    (*p)++;
}

#include <iostream>
#include "funcoes.h"
using namespace std;

int main() {
    int x = 5, y = 10;
    trocar(x, y);
    cout << x << " " << y << endl;
    incrementar(&x);
    cout << x << endl;
}
