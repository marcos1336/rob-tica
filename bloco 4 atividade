16.

cpp
Copiar
Editar
void trocar(int &a, int &b) {
    int temp = a;
    a = b;
    b = temp;
}
17.

cpp
Copiar
Editar
void alterar(int valor, int &ref) {
    valor += 5;
    ref += 5;
}
18.

cpp
Copiar
Editar
void incrementar(int* p) {
    (*p)++;
}
19.

cpp
Copiar
Editar
float media(float n1, float n2, float n3) {
    return (n1 + n2 + n3) / 3;
}
20.

cpp
Copiar
Editar
void situacao(string nome, float nota) {
    cout << nome << ": " << (nota >= 7 ? "Aprovado" : "Reprovado") << endl;
}
